import { Oswald, Roboto_Slab, Roboto } from 'next/font/google';

export const oswald = Oswald({ subsets: ['latin'] });

export const roboto_slab = Roboto_Slab({ subsets: ['latin'] });

export const roboto = Roboto({ subsets: ['latin'], weight: ['400', '500'] });